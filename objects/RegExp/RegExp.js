var re1 = /ab+c/g; //global
var re2 = /ab+c/i; //Case-insensitive search.
var re3 = /ab+c/m; //Multi line
//var re4 = /ab+c/y; //Perform a "sticky" search that matches starting at the current position in the target string.

var re = new RegExp("ab+c");
var re = new RegExp("ab+c","g");

// The exec() method executes a search for a match in a specified string. Returns a result array, or null.


//String: match
//The match() method retrieves the matches when matching a string against a regular expression.
//serch, replace ,split
//Regexp exec
